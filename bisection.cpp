#include <iostream>
#include <cmath>

using fp = double(double);
double bisection(double a, double b,double eps, int N,fp f);
double g(double x);

int main

{
  std::cout.presicion(16);
  std::cout.stef(std::ios::scientific)

  const double eps=1.0e-9;
  int N=30
    double a = 12,b = 18, 

    
  return 0;
}

double bisection(double a, double b, int N,fp f);
{

  double fc(0);
  double a();  
  double b();
  double xr=0.5*(a+b);
  for (double x=a; x=b;x++){
    fc=
  }
  
}
